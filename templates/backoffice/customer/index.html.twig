{% extends 'backoffice/base.html.twig' %}

{% block title %}Clients{% endblock %}

{% block body %}
    {% if customers is not empty %}
        <div class="card">
            <div class="card-body">
                <div class="p-4">
                    <div class="grid-12 -noGutter">
                        <div class="col-8_sm-12">
                            <div class="form-group form-icon form-icon-left mb-0">
                                <div class="flex">
                                    <div class="pl-3 pr-3 flex align-items-center"><i class="ri-search-2-line"></i></div>
                                    <div class="flex-root">
                                        <input type="text" name="key" autocomplete="off" onkeyup="searchHttpRequest(this.value, '/admin/customers/ajax/search')" class="form-input" id="txtHint" placeholder="Rechercher un client">
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-4_sm-12">
                            <div class="ml-4 text-right">
                                <a href="" class="btn btn-purple m-0"><i class="fs-18px ri-add-fill"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="results" id="htmlContent"></div>
            </div>
        </div>
        <div class="grid-12 mt-5" id="table-visible">
            {% for c in customers %}
                <div class="col-3_sm-12">
                    <a class="color-inherit p-relative" href="{{ url_for('customer_read', {'id': c.id}) }}">
                    <span class="pin pin-top pin-right">
                    {% if c.enable_portal is not null %}
                        <span class="label badge-light-green">
                        <i class="ri-lock-unlock-line"></i>
                        </span>
                    {% endif %}
                        {% if c.on_sale is not null %}
                            <span class="label badge-light-green">-{{ c.on_sale }}%</span>
                        {% endif %}
                    </span>
                        <div class="card {% if c.is_blacklisted is not null %}border-bottom-pink{% endif %}">
                            <div class="card-body p-4">
                                <div class="flex align-items-center">
                                    <div class="mr-3">
                                        <span><i class="fs-32px ri-{% if c.is_society is not null %}building{% else %}user-2{% endif %}-fill"></i></span>
                                    </div>
                                    <div>
                                        <h4>{{ c.fullname }}</h4>
                                        <p class="m-0">
                                    <span class="d-block">
                                        Téléphone : {{ c.phone }}
                                    </span>
                                            <span class="d-block">
                                        Ville : {{ c.zipcode }} - {{ c.city }}
                                    </span>
                                            <span class="d-block">
                                        Courriel : <strong>{{ c.mail }}</strong>
                                    </span>
                                            <span class="d-block">
                                        Ajouté le : {{ c.created_at | date('d/m/Y') }}
                                    </span>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="text-center">
        <span>
            <i class="fs-80px ri-user-line"></i>
        </span>
            <h3>Aucun client</h3>
            Ajoutez des clients et retrouvez-les ici
            <div class="mt-3">
                <a href="" class="btn btn-light-purple"><i class="ri-add-line"></i> Nouveau client</a>
            </div>
        </div>
    {% endif %}
{% endblock %}

{% block javascripts %}
    <script src="/assets/js/search.js"></script>
{% endblock %}
