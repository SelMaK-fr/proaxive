<div class="modal" id="add_document">
    <div class="modal-container">
        <div class="modal-header">
            <h1>Téléverser un nouveau document</h1>
        </div>
        <form action="{{ url_for('document_add') }}?c={{ i.customers_id }}&i={{ intervention_id }}" method="post" id="form_deposit" enctype="multipart/form-data">
            <div class="modal-content">
                <div class="mb-4">
                    Ce document sera ajouté à l'intervention en cours.
                    <p>
                        Mode public : le document sera visible dans l'espace client.
                    </p>
                </div>
                <div class="grid-center">
                    <div class="col-6_sm-12">
                        <div class="form-group">
                            <label for="" class="form-label">Sélectionnez un fichier</label>
                            <input type="file" id="avatar" name="form_document[file]" />
                        </div>
                        <div class="form-group">
                            <label for="" class="form-label">Nom du document/fichier</label>
                            <input name="form_document[name]" id="" type="text" class="form-control">
                        </div>
                        <div class="form-group">
                            <label class="form-label" for="form_document-is_online">Mode public</label>
                            <input type="checkbox" name="form_document[is_online]" id="form_document-is_online">
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer text-right">
                <button type="submit" class="btn btn-light-four">Téléverser</button>
                <button data-dismiss="modal" class="modal__close btn btn-light-pink">Annuler</button>
            </div>
        </form>
    </div>
</div>