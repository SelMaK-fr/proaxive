{% extends '_layout/dashboard/layout.twig' %}

{% block title 'Gestion des équipements' %}

{% block body %}
    <div class="section--admin">
        <div class="container">
            <form method="post">
        {{ include('_notifications.twig') }}
            <div class="card-custom mg-b-3">
                <div class="card-body">
                    <div class="card-header m-0">
                        <div class="columns">
                            <div class="col-8 col-sm-12"><h2>Nouvel équipement</h2><span>Création d'un nouvel équipement pour un client</span></div>
                            <div class="col-4 col-sm-12 text-right">
                                {{ form.submitDefault('Sauvegarder', 'primary') | raw }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="admin-content">
                {{ include('equipments/admin/_form.twig') }}
            </div>
            </form>
        </div>
    </div>
    <!-- END .cards -->
{% endblock %}